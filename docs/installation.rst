============
Installation
============

At the command line::

    $ easy_install django_eulasees

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_eulasees
    $ pip install django_eulasees
